# Fanbasemarket Site

## System Requirements
* Python 3.7
* npm
* Any version of MySQL

## Setup Instructions
* `pip install -r requirements.txt`
* `python setup.py install`
* `pip install -e .`
* `flask run`
